package frc.robot.rookiecoding

import frc.chargers.framework.ChargerRobot

/**
 * TODO: Finish this class.
 * Tasks:
 * 1. Instantiate the drivetrain
 * 2. set the default command to a RunCommand that drives the drivetrain off a controller
 * 3. Add a deadband(MathUtil.applyDeadband) of 0.2 for all inputs
 */
class RookieBot: ChargerRobot() {
}